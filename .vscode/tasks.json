{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run",
            "type": "shell",
            "command": "cmake -Bclass-methods/build -Hclass-methods && cmake --build class-methods/build && ./class-methods/build/MyProject",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
